spec:
  name: stiv-hugo-blog-page
  static_sites:
  - build_command: rm -r ./public; hugo --destination ./public
    environment_slug: hugo
    git:
      branch: main
      repo_clone_url: https://github.com/StivDC/stivdc.uk
    name: stiv-hugo-blog-page
